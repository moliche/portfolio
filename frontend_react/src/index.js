import React from 'react'
import ReactDom from 'react-dom'

import App from './App'
import './index.css'

ReactDom.render(<App />, document.getElementById('root')); //connects the React app straight inside the div with id of 'root' 